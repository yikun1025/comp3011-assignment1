# Offline profile: activates StubSpeechToTextService instead of the real OpenAI client.
# No API key is needed. Enable with --spring.profiles.active=stub,
# or @ActiveProfiles("stub") in tests.

# Simulated Cloud latency, so concurrency tests exercise genuinely blocking requests.
app.stub.delay=200ms